# macOS 메타데이터 파일 제외

## 배경

문서 폴더를 확인하는 과정에서 macOS가 생성한 `.DS_Store` 파일이 프론트엔드 작업 트리에 나타났다. 프로젝트와 무관한 파일이 실수로 커밋되지 않도록 제외 규칙이 필요했다.

## 구현 방식

- `.gitignore`에 `.DS_Store`를 추가했다.

## 영향 범위

- `.gitignore`

## 검증 내용

- `git status --short`에서 `.DS_Store` 파일이 더 이상 나타나지 않는지 확인했다.

## 남은 리스크

- 없음
